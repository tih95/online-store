import styled from 'styled-components';

export const CartItemCount = styled.p`
  padding: 3px;
  background: #d63031;
  border-radius: 50%;
  color: white;
  position: absolute;
  top: -10px;
  right: -10px;
`;